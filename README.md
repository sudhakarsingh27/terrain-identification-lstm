## Terrain identification using LSTM

#### `Data` contains training and test data

#### `experiment_lstm` contains the code to preprocess data and train an LSTM based neural network to predict the terrain